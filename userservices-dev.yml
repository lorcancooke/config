example:
  property: IAMTHEDEV
server:
  port: 8081
spring:
  application:
    name: userservices
  profiles:
    active: dev

  management:
    endpoints:
      web:
        exposure:
          include: 'refresh'
my:
  greeting: Hello World
  list:
    values: One, Two, Three
db:
  connection: connection-string-here
  host: 127.0.0.1
  port: 1200
